---
title: "AuricularWorld: Hierarchical Action-Guided World Modeling for Fine-Grained Auricular Structure Segmentation from CT Scans"
title_zh: AuricularWorld：面向CT扫描细粒度耳廓结构分割的分层动作引导世界建模
authors: "Jingwen Yang, Senmao Wang, Luoyao Kang, Runmeng Cui, Keying Zhang, Yunjia Bao, Haifan Gong, Lin Lin, Haiyue Jiang"
date: 2026-07-30
pdf: "https://arxiv.org/pdf/2607.28487v1"
tags: ["query:abdk-seg"]
score: 8.0
evidence: 提出基于世界模型的框架用于CT中精细耳部结构分割，是一种医学图像分割深度学习模型。
tldr: "CT图像中耳廓结构细小、边界不规则且与软组织界限模糊，存在重叠嵌套的标签标注难题。本文提出AuricularWorld框架，引入世界模型在潜在空间进行迭代解剖推理，通过层次动作逐步优化分割。实验表明该方法显著提升了分割精度，HD95指标降低超过43%。研究成果验证了潜在世界模型推理在复杂医学图像分割中的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.28487v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1257, \"height\": 517, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.28487v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1252, \"height\": 745, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.28487v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1254, \"height\": 648, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.28487v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1253, \"height\": 669, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.28487v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1260, \"height\": 881, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.28487v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 991, \"height\": 488, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28487v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1019, \"height\": 613, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28487v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1023, \"height\": 359, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28487v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1264, \"height\": 1186, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.28487v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1270, \"height\": 1033, \"label\": \"Table\"}]"
motivation: 解决CT中耳廓结构细小、边界模糊且标签重叠嵌套的精准分割难题。
method: 提出AuricularWorld框架，在编码器-解码器中嵌入循环状态空间模型，通过层次动作驱动的潜在迭代细化分割。
result: "在CT数据上，小、不规则、重叠的耳廓结构分割精度持续提升，HD95降低超43%。"
conclusion: 验证了潜在世界模型迭代推理在挑战性医学图像分割中的有效性和潜力。
---

## 摘要
CT中耳廓结构的细粒度分割充满挑战，因为耳朵占据的图像区域很小，软骨边界高度不规则，且软骨与周围软组织之间的界面常常模糊不清。临床标注还可能同时包含软骨及相邻皮肤组成的复合结构及其对应的纯软骨区域，产生嵌套和重叠的标签。我们提出了一种基于世界模型的分割框架，能够超越传统的前馈预测进行迭代的解剖学推理。该框架基于编码器-解码器架构，在中间潜在空间中引入一个确定性循环状态空间模型。融合多尺度编码器特征与部分解码的表征，形成结构观察，用以初始化潜在动态。在推理过程中，模型在没有真实指导的情况下执行三步潜在展开。分层解剖动作更新循环状态并逐步细化潜在表征。所得的潜在轨迹被投影回解码器，并与高分辨率特征结合，生成最终的分割结果。为了学习可靠的潜在状态转移，我们引入了一个平衡的分层动作目标，该目标应对了前景稀疏性、解剖组缺失以及增加与移除操作之间的不平衡。大量实验表明，所提出的框架能够持续提升分割精度，并将CT中小型、不规则和重叠耳廓结构的HD95降低超过43%。这些结果证明了潜在世界模型推理对于挑战性医学图像分割的有效性。

## Abstract
Fine-grained segmentation of auricular structures in CT is challenging because the ear occupies a small image region, cartilage boundaries are highly irregular, and interfaces between cartilage and surrounding soft tissues are often ambiguous. Clinical annotations may also include both composite structures containing cartilage and adjacent skin and their corresponding cartilage-only regions, producing nested and overlapping labels. We propose a world-model-based segmentation framework that enables iterative anatomical reasoning beyond conventional feed-forward prediction. Built on an encoder-decoder architecture, the framework introduces a deterministic recurrent state-space model into the intermediate latent space. Multi-scale encoder features and partially decoded representations are fused to form a structural observation that initializes the latent dynamics. During inference, the model performs a three-step latent rollout without ground-truth guidance. Hierarchical anatomical actions update the recurrent state and progressively refine the latent representation. The resulting latent trajectory is projected back into the decoder and combined with high-resolution features to produce the final segmentation. To learn reliable latent transitions, we introduce a balanced hierarchical action objective that addresses foreground sparsity, missing anatomical groups, and imbalance between add and remove operations. Extensive experiments show that the proposed framework consistently improves segmentation accuracy and reduces HD95 by more than 43% for small, irregular, and overlapping auricular structures in CT. These results demonstrate the effectiveness of latent world-model reasoning for challenging medical image segmentation.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **核心问题**：CT 中细粒度耳廓结构分割面临三大挑战：
    - 耳朵占整张头颈 CT 的体积极小，导致严重的前景 - 背景不平衡；
    - 软骨边界形状极不规则，与周围软组织的对比度低，难以精确描绘；
    - 临床标注中存在嵌套和部分重叠的复合解剖结构（如皮肤覆盖的亚单位与其对应的纯软骨区域），传统的互斥多类分割难以直接处理。
- **整体含义**：该工作提出了一种全新的分割思路，将耳廓分割从一个“单步前馈映射”问题重新定义为 **“潜在解剖学演化过程”** 。通过引入世界模型的理念，让一个可观察的解剖学假设在潜在空间中通过一系列可解释的“解剖动作”（添加/移除）逐步迭代修正，最终达到结构一致且边界精确的分割，从而克服单纯依靠前馈网络一次性预测的局限。

### 2. 论文提出的方法论

- **核心思想**：名为 **AuricularWorld** 的框架，在现有的 `nnU-Net` 编码器 - 解码器架构中，于中分辨率阶段插入一个**循环状态空间模型 (RSSM)**，将分割视为在潜在空间中的多步推理过程。
- **关键技术细节**：
    - **多尺度解剖观测**：融合编码器多级特征 (`E0`-`E4`)、瓶颈层 (`B`) 以及部分解码的特征 (`D2`)，经过 `1×1×1` 卷积对齐和 `3×3×3` 卷积融合，形成**一个结构观测 `o`**，用于初始化潜在状态。
    - **动作-条件 RSSM 潜在展开**：
        - 由确定性状态 `h` 和随机状态 `z` 构成，通过 `o` 初始化。
        - 进行 **T=3 步**的递归更新。每步中，一个 **Actor** 预测**分层解剖动作** `â` （add/remove），动作与上一时刻的随机状态一起输入 **ConvGRU** 更新确定性状态，再通过先验/后验网络产生新的随机状态。
        - 推理时，**不使用真实标签指导**，仅依靠图像特征估计的状态进行展开。
    - **分层动作学习**：定义 **73 个解剖组**（35 个原子组、35 个规范组、3 个全局组），动作目标是当前预测与真实标签之间的连续差异（add 目标为真实但未预测到的区域，remove 为预测错误区域）。
    - **前景掩码的平衡动作损失**：
        - **通道级平衡**：根据解剖层级、分组是否存在、动作类型（add/remove）分配不同的权重，缓解类别和操作类型不平衡。
        - **空间级掩码**：通过膨胀真实前景和预测高置信前景的并集，对背景体素施加极低权重，避免大量负样本淹没监督信号。
    - **训练损失**：总损失 = 最终分割损失 + 0.2 × 递归辅助分割损失 + 0.1 × KL 散度正则化 + 0.2 × 动作损失。
- **流程概要**：图像 → 编码器 → 多尺度观测 `o` → RSSM 三步动作引导的潜在状态演进 → 将精炼后的潜在特征 `r` 投影回解码器，与高分辨率跳连结合 → 输出最终原子级分割掩码。

### 3. 实验设计

- **数据集**：
    - **自建的细粒度耳廓 CT 数据集**：包含 193 例患者共 198 个已标注耳廓，扫描机器为 Philips CT 6000 和 Brilliance 64。
    - **标注特点**：提供皮肤覆盖亚单位与对应软骨的互补双轨标注，并被转化为 **35 个互斥的原子标签**，评估时再重组为 35 个规范解剖结构。
    - **划分**：严格按患者级别划分为训练集（117 例）、验证集（41 例）和测试集（32 例）。
- **基准方法**：
    - `nnU-Net`、`TransUNet`、`nnFormer`、`nnMamba`（基于 CNN、Transformer 和状态空间模型的代表作）。
    - `UNETR`、`SwinUNETR`（采用 MONAI 官方管线实现）。
- **评价指标**：
    - 宏观平均 Dice 相似系数（%）。
    - 95% 豪斯多夫距离 `HD95` (mm)，按物理空间计算。

### 4. 资源与算力

- **硬件**：单张 NVIDIA GeForce RTX 4090 GPU（24 GB 显存）。
- **软件**：Python 3.10，PyTorch 2.12.0，CUDA 12.6。
- **训练设置**：
    - 基于 nnU-Net 管线的模型（含 AuricularWorld）训练 500 个 epoch，每 epoch 250 次训练迭代，批次大小为 2。
    - 使用带动量的 SGD，初始学习率 1e-2，权重衰减 3e-5，多项式学习率衰减，自动混合精度。
    - 论文未提供总训练时长或每次实验的耗时的精确数值。

### 5. 实验数量与充分性

- **实验组数较多且较为系统**：
    - **与主流方法的外部比较**：在固定的测试集上比较了 6 种现有方法，每种方法使用 3 个不同的随机种子独立训练并报告均值和标准差，还在 35 个解剖结构上统计了“最佳结构数”。
    - **增量组件消融实验**：从 `nnU-Net` 基线出发，逐步添加多尺度动作 RSSM (MS-RSSM)、平衡动作加权 (Bal.)、前景掩码动作监督 (FG)，共 3 种变体加上完整模型。
    - **分类别性能分析**：按结构物理体积大小、尺寸调整后的边界复杂度、组织类型（软骨/非软骨）进行分层比较。
    - **结构级细粒度对比**：列出了所有 35 个解剖结构的 Dice 和 HD95，详细对比 `nnU-Net` 与 AuricularWorld。
    - **定性可视化**：展示了分割结果对比图、潜在展开过程中动作地图的演化图。
    - **超参数敏感性分析**：对区域权系数和动作损失权系数进行调参实验。
- **充分性与公平性**：
    - 实验设计较为充分，通过消融和细粒度统计充分验证了各组件的贡献。所有方法使用相同的原子标签、训练/验证/测试集划分和评价逻辑，保证了公平性。
    - 对于 Transformer 类方法使用了各自官方推荐的不同预处理和增强管线，虽属常见实践，但可能引入部分因素差异（如体素间距和归一化），本文已明确说明。

### 6. 论文的主要结论与发现

- AuricularWorld 在测试集上取得了 **78.42% 的 Dice** 和 **1.379 mm 的 HD95**，显著优于所有对比方法。
- 与 `nnU-Net` 相比，**Dice 提升 0.67 个百分点，HD95 相对降低约 43.2%**。其对边界误差的改善尤为突出。
- 消融实验揭示，**MS-RSSM 是主要的性能增益来源**（HD95 相对基线骤降约 42%），而平衡加权和前景掩码则贡献了进一步的边界优化和稳定性提升。
- 模型对**小体积结构、复杂边界结构以及软骨组织**的改进最为明显，证明了潜在级推理擅长处理不规则、低对比度的精细解剖实体。
- 可视化分析显示，动作地图在首步主要恢复缺失结构，后续步则用于抑制过度响应和稳定边界，整个推理过程在空间上高度聚焦。

### 7. 优点

- **新颖的建模视角**：首次将“世界模型”明确引入医学图像分割，用迭代、动作驱动的潜在演化替代单次映射，为处理复杂嵌套标签提供了新范式。
- **精巧的动作设计**：
    - 将分割修正量化为分层的“添加/移除”动作，不仅提供了结构化的监督，还使模型的演化过程具有可解释性。
    - 针对前景稀疏、类别缺失等实际问题的前景掩码平衡损失，设计细致且有效。
- **框架设计优雅**：在特征空间完成时序推理，同时保留了原有高分辨率解码器，巧妙地避免了直接迭代掩码可能引起的边界模糊或细节丢失。
- **实验全面扎实**：自建针对性的细粒度数据集，从宏观指标到结构级、分类别的系统性评估，辅以大量消融、可视化和超参数分析，论证充分且对比公正。

### 8. 不足与局限

- **数据集的局限**：
    - 仅在自有的单中心数据集上验证，样本量（193 例）和采集设备种类有限，缺少多中心、大样本的外部验证，泛化能力未知。
    - 标注依赖资深专家，成本高昂，且排除了严重伪影或显影不全的扫描，可能偏向了质量较高的数据，临床应用中的鲁棒性仍需检验。
- **方法对比的广度**：
    - 基线方法虽具代表性，但缺少与同样采用 **迭代优化或蒙版精炼思想**的方法（如基于循环网络或自校正的分割方法）的直接比较，不易判读“世界模型”范式相较于其他迭代策略的独特优势。
    - 未进行任何统计显著性检验，仅报告了三次运行的均值与标准差。
- **系统的复杂性与效率**：
    - 框架相比 `nnU-Net` 引入了额外的循环状态模型和多个辅助目标，增加了模型复杂度和训练开销（更重的损失计算），但论文未提供推理时间、参数量或 FLOPs 的增加情况，限制了实时性或轻量化部署的评估。
- **动作定义的特异性**：73 个解剖组的定义和层级权重分配（如原子层 0.6，规范层 0.3）是人为设定，可能在迁移至其他器官或标签体系时需要重新设计并调整超参数。

（完）
