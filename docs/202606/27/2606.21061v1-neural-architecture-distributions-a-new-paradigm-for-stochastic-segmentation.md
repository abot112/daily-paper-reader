---
title: "Neural Architecture Distributions: A New Paradigm for Stochastic Segmentation"
title_zh: 神经架构分布：随机分割的新范式
authors: "Conghui Li, Junhao Huang, Chern Hong Lim, Bing Xue, Mengjie Zhang"
date: 2026-06-19
pdf: "https://arxiv.org/pdf/2606.21061v1"
tags: ["query:abdk-seg"]
score: 9.0
evidence: 新型分割范式，使用架构分布进行随机分割，适用于医学图像
tldr: 安全关键应用中，随机分割需生成多个可信掩码，但现有方法依赖连续潜变量或去噪轨迹，其随机源难以搜索和审计。本文提出神经架构分布新范式，将随机性转化为采样离散架构：从学习到的操作选择分布中抽取架构，通过激活路径产生唯一掩码并支持架构溯源。训练时引入集合级监督，以基于IoU的能量距离替代损失匹配架构采样集与专家标注集，并利用进化搜索预先构建丰富可优化的候选操作库。在LIDC-IDRI肺结节分割数据集上，该方法实现了最先进的分布匹配和假设覆盖，并成功扩展至两个额外任务。这是首次将随机分割形式化为架构分布学习，通过架构采样实现可审计的输出多样性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21061v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1246, \"height\": 712, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21061v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1247, \"height\": 425, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21061v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1241, \"height\": 358, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21061v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1248, \"height\": 448, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21061v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1248, \"height\": 783, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21061v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 781, \"height\": 513, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21061v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1719, \"height\": 722, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21061v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 797, \"height\": 474, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21061v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1714, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21061v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1712, \"height\": 312, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21061v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 945, \"height\": 335, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21061v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 665, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21061v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1711, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21061v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1712, \"height\": 223, \"label\": \"Table\"}]"
motivation: 现有随机分割方法通过连续潜变量或去噪引入随机性，其随机源难以搜索和审计，限制安全关键应用的可信度。
method: 提出神经架构分布，通过学习操作选择分布并采样离散架构产生多样性；训练利用集合级监督与IoU-based能量距离替代损失，并进化搜索构建候选库。
result: 在LIDC-IDRI数据集上，方法在分布匹配和假设覆盖上达到SOTA，并成功应用于两个扩展任务。
conclusion: 首次将随机分割定义为架构分布学习，通过架构采样实现可审计的输出多样性，为随机分割提供新范式。
---

## 摘要
随机分割旨在为单张图像生成多个合理的掩膜，这在医学影像或建筑缺陷检测等安全与质量关键应用中至关重要。现有方法大多通过注入连续潜变量或迭代去噪轨迹来引入随机性，但其随机来源难以直接搜索或审计。我们提出将架构分布作为分割的新随机来源：不是采样潜变量或噪声，而是从分割骨干网络中多个可搜索位置上的算子选择所学习的分布中采样离散架构。每个采样架构通过所选的活跃路径生成一个掩膜，因此推理依赖于执行的子网而非完整的候选库。该方法还支持架构溯源，因为每个输出对应一个特定的架构配置。为减少向平均掩膜塌缩，我们采用集合级监督，通过基于IoU的能量距离代理，将一组架构采样预测与标注集进行匹配。我们进一步通过进化搜索构建候选库，使得随机来源的支持在分布学习之前可优化。所提方法在LIDC-IDRI数据集上实现了最先进的分布匹配和假设覆盖，并在两个扩展任务上保持有效。据我们所知，这是首次将随机分割形式化为学习架构分布并通过架构采样实现输出多样性的工作。

## Abstract
Stochastic segmentation seeks to represent multiple plausible masks for a single image, which is essential in safety- and quality-critical applications such as medical imaging or building defect inspection. Most existing methods introduce stochasticity by injecting continuous latent variables or by iterative denoising trajectories, whose stochastic sources are difficult to search or audit directly. We propose architecture distributions as a new stochastic source for segmentation: instead of sampling a latent variable or noise, we sample a discrete architecture from a learned distribution over operator choices at multiple searchable positions in a segmentation backbone. Each sampled architecture yields one mask through the selected active path, so inference depends on the executed subnet rather than the complete candidate bank. This approach also supports architectural provenance, since each output corresponds to a specific architecture configuration. To reduce collapse toward averaged masks, we train with set-level supervision by matching a set of architecture-sampled predictions to the annotation set using an IoU-based energy-distance surrogate. We further construct the candidate bank with evolutionary search, making the support of the stochastic source optimizable before distribution learning. The proposed method achieves state-of-the-art distribution matching and hypothesis coverage on LIDC-IDRI, and remains effective on two extension tasks. To the best of our knowledge, this is the first work to formulate stochastic segmentation as learning an architecture distribution and realizing output diversity through architecture sampling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究动机**：在医学图像分析、建筑缺陷检测等安全关键应用中，由于图像证据不足、边界模糊或标注者的主观性，单个输入图像通常存在多个合理的分割掩膜。因此，仅输出单一“平均”解的传统确定性模型难以满足需求，需要能够建模条件分布、生成多个合理假设的随机分割方法。
*   **现有方法的局限**：主流的随机分割方法（如基于CVAE、扩散模型或正态化流的方法）主要通过注入连续的潜变量或利用迭代去噪轨迹来引入随机性。这带来了两个主要问题：
    *   **随机源不可搜索/审计**：随机性来源于连续空间，难以直接搜索、解释或追踪，在安全关键场景下缺乏可信度。
    *   **推理成本高**：扩散模型需要多步采样，基于流的方法增加了额外的变换层，导致推理效率低下。
*   **论文核心思想**：本文提出了一种全新的范式——**神经架构分布 (Neural Architecture Distributions)**。其核心创新在于将随机分割的随机性来源从“潜变量/噪声”转变为“离散的架构选择”。模型不是注入噪声，而是学习一个给定输入下的架构分布，并通过采样不同的网络架构来生成多样化的分割掩膜。

### 2. 论文提出的方法论

*   **核心思想**：在分割网络的特定位置（搜索块）定义一组候选算子（如不同卷积核大小、膨胀率、残差连接等），并学习一个输入条件化的架构分布。每次推理时，从该分布中采样一个离散架构，仅执行该架构对应的活跃路径，从而产生一个分割掩膜。
*   **关键技术细节**：
    *   **架构变量定义**：在U-Net解码器的多个阶段插入可搜索块，每个块包含一个候选算子库。一个完整的架构 `a` 由各位置选择的算子索引构成。
    *   **架构分布学习**：训练一个控制器 `g_ψ` ，根据输入图像 `x` 的特征 `h(x)` 为每个搜索位置预测选择各算子的logit，通过Softmax形成候选算子的分类分布 `q_ψ(a|x)`。
    *   **加权共享与采样**：所有可能的架构共享同一套网络权重（超网）。推理时，通过Gumbel-Softmax重参数化技巧可微地采样架构 `a`，并执行其对应的单路径子网 `f_θ(x; a)` 生成预测 `ŷ(x; a)`。
    *   **训练目标函数**：为避免模型塌缩为单一平均解，本文设计了集合级监督损失。
        *   **能量距离代理损失**：核心损失 `Lset` 基于广义能量距离 (GED)，通过计算预测集与标注集之间的IoU距离，鼓励预测集既能覆盖所有标注模式（交叉项 `d̄_PY`），又能保持样本间的多样性（自项 `d̄_PP`）。
        *   **辅助损失与正则化**：加入标准的BCE-Dice损失 (`Laux`) 作为训练稳定器，并对架构分布施以负熵正则项(`R(qψ)`)，防止其过早收敛为确定性策略。
    *   **候选库进化搜索**：在训练架构分布之前，采用“老化进化 (Aging Evolution)”算法，以GED和计算复杂度为适应度，在超网中搜索性能优异的候选算子组合，为每个位置构建一个精简的、面向性能的Top-N候选库。这实现了对随机源支持集的优化。
*   **算法流程**：
    1.  **超网预热**：随机采样架构，仅用集合级损失训练超网共享权重。
    2.  **进化搜索**：冻结架构参数，通过进化算法搜索并筛选出每个位置的Top-N个高效候选算子。
    3.  **端到端训练**：在筛选后的候选库上，联合优化超网权重 `θ` 和架构控制器参数 `ψ`，使用包含 `Lset`、`Laux` 和 `R(qψ)` 的完整损失函数。

### 3. 实验设计

*   **数据集与场景**：
    *   **主实验：随机分割**
        *   **LIDC-IDRI**：肺部CT结节分割数据集，包含多位放射科专家的标注，是评估标注歧义性的标准基准。
    *   **扩展实验：确定性分割**
        *   **Cityscapes**：多类别城市场景语义分割数据集，验证方法在标准任务上的竞争力。
        *   **Crack500**：道路裂缝分割数据集，验证方法在极度不平衡、薄结构目标上的鲁棒性。
*   **评估指标**：
    *   **LIDC-IDRI**：广义能量距离 (GED，越低越好) 和标准化互信息交并比 (HM-IoU，越高越好)，评估预测分布与多位专家标注分布的匹配度。
    *   **Cityscapes**：交并比 (IoU)。
    *   **Crack500**：精确率 (Precision)、召回率 (Recall)、F1分数。
*   **对比方法**：与众多代表性随机分割方法进行了全面比较，涵盖了不同技术路线的SOTA方法：
    *   **CVAE系**：Probabilistic U-Net, PHiSeg, HProb. U-Net, JProb. U-Net, GMM_NF。
    *   **其他潜变量/结构化方法**：SSN, cFlow, CAR, PixelSeg, MoSE。
    *   **扩散模型系**：CIMD, CCDM, AB。

### 4. 资源与算力

*   **显式说明**：论文正文和补充材料中**未明确提及**用于实验的具体GPU型号与数量。
*   **间接提及与推理**：
    *   **训练时长对比**：在补充材料表7中，论文直接比较了不同方法的单epoch训练时间（如Prob. U-Net为2分28秒，Ours为9分42秒），并明确指出NADS的训练成本（约为基础方法的3-4倍）是最高的，归因于其需要训练包含庞大候选算子库的超网。
    *   **推理效率**：论文表4详细比较了推理时的效率，强调NADS的推理成本由采样的**活跃子路径**决定（参数量26M，低于多数基线），而非完整的超网（387M）。因此，虽然训练开销大，但部署时的推理效率与主流方法相当（远优于扩散模型）。
    *   **算力需求结论**：该方法的代价在于离线训练阶段更高的计算资源需求，以换取推理时的高效率、分布匹配精度与可审计性。

### 5. 实验数量与充分性

*   **实验数量**：设计了丰富的实验组合，覆盖不同任务、多维度指标和消融分析。
    *   **多数据集验证**：在3个不同性质的数据集上进行了验证。
    *   **多指标对比**：与超过12种相关方法在多个K值（16/32/50/100）设定下进行了GED和HM-IoU的详细对比。
    *   **效率分析**：对比了6种方法的参数量、理论复杂度与实测推理时间。
    *   **消融研究**：
        *   分离了**架构采样机制**与**集合级目标函数**的各自贡献（对比了几种替代损失和固定架构/无采样机制）。
        *   验证了用传统单标注损失或纯进化策略学习架构分布的失败模式（分布坍缩）。
    *   **容量与温度分析**：在补充材料中对搜索位置数、候选库大小、Gumbel-Softmax温度策略进行了详细的消融分析和参数敏感性验证。
*   **充分性与公平性评估**：
    *   **充分性**：实验设计非常充分、系统。不仅证明了方法的有效性，还通过全面的消融实验清晰地阐述了新范式（架构采样+集合级监督）各组件的作用和必要性。
    *   **客观公平性**：与SOTA方法的对比遵循了文献`[8]`的基准设置，并引用了其公布的结果，保证了比较的公平性。在多个实验中都报告了标准差，显示了结果的稳定性。

### 6. 论文的主要结论与发现

1.  **架构分布是有效且优越的随机源**：该范式能成功地将随机分割建模为架构采样问题，在LIDC-IDRI数据集上，其在分布匹配（GED）和假设覆盖（HM-IoU）两个核心指标上均取得了最优性能，显著超越了基于潜变量和扩散模型的方法。
2.  **集合级监督和架构采样缺一不可**：消融实验证明，仅使用传统的像素级损失（如Dice, BCE）会导致架构分布坍塌为确定性模型，无法产生多样性；而仅有GED损失却没有架构采样机制的固定网络也无法生成好结果。二者结合是新范式的关键。
3.  **可搜索的随机源带来性能提升与可审计性**：通过进化算法预先优化候选算子库，可以有效塑造随机源的支持集，提升最终分布学习的表现。同时，每个输出都对应一个明确的离散架构，提供了天然的“溯源”能力，增强了模型在安全关键场景中的可信度。
4.  **泛化能力与效率平衡**：该框架不仅限于随机分割，也能在确定性分割任务（Cityscapes, Crack500）上取得有竞争力的结果。训练成本虽高，但推理效率与轻量级方法相当，实现了部署效率与输出多样性的良好平衡。

### 7. 优点

*   **范式创新性强**：彻底改变了随机分割的实现方式，将随机源从抽象的潜变量空间转移到可解释、可操作的离散架构空间。
*   **可解释性与可审计性**：输出多样性与具体的网络结构直接挂钩，为其生成路径提供了“架构溯源”，这在安全关键领域是重要的潜在优势。
*   **方法设计系统且完善**：将进化搜索（塑造随机源）、集合级损失（训练目标）与架构采样（生成多样性）有机结合，形成了一个完整、自洽的框架。
*   **性能表现优越**：在主要的随机分割基准测试上达到了SOTA水平，且优势明显。
*   **推理效率高**：虽然训练开销大，但在推理时仅执行单一路径，避免了扩散模型的多步迭代或复杂变换，平衡了性能与部署成本。

### 8. 不足与局限

*   **训练成本显著偏高**：无论是超网预热、进化搜索还是最终的端到端训练，其时间和计算资源消耗远超对比的基线方法（如文中显示的单epoch训练时间约为基线的3-4倍）。
*   **架构空间的有限性**：输出多样性被限制在预定义的、离散的候选算子组合中（`|A|=ΠCℓ`）。当一个极其罕见或全新的合理掩膜模式无法被任何现有架构组合所表达时，模型将无法生成该模式。
*   **超网设计的复杂性**：方法引入了额外的超参数（如搜索位置数L、候选库大小N、各种损失权重、进化算法参数等），增加了方法落地时的调参复杂度。
*   **实验覆盖的局限性**：尽管在多个数据集上验证，但主打的“架构溯源”优势在文中更多是定性阐述，缺乏定量的、用户导向的审计或可解释性评估实验来有力支撑其声明。

（完）
